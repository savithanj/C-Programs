// 5.	Write a C program to accept an integer value from the user and display with appropriate message.

#include<stdio.h>

int main()
{
  int a;

  printf("Enter an integer :");
  scanf("%d", &a);

  printf("a = %d\n",a);
  
  return 0;
}
